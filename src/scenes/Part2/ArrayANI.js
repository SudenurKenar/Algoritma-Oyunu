import Phaser from 'phaser';
import { Theme } from '../../core/theme.js';

// Sınıfın başında mutlaka 'export' olmalı ve ismi 'ArrayANI' olmalı
export class ArrayANI extends Phaser.Scene {
    constructor() {
        super('ArrayANI');
    }
    // ... create ve animasyon kodları
}